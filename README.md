# static-next-js

This example uses Next.js v15:

```sh
npx create-next-app@15 next
```

Before generating the static export, ensure the backend server is running:

```sh
cd express
npm start
```

Then build the static export and start the static server:

```sh
npm run build:export
npm run start:export
```
